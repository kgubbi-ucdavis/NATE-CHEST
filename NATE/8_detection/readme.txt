* you should run the script in "7_train_NN" to predict the label for all timing paths.
* This script uses the prediction result of both MLP and Stacked models to first, detect trojans, second add them all in a dataset so later you can compare them.
* this script also generates the required results for diagnostics and finding the victimized nets

edit the config file
run the top.py

* there are misc. scripts for plotting, histogram, etc... in the Misc folder.